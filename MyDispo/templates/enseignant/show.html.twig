{% extends 'base.html.twig' %}

{% block title %}Enseignant{% endblock %}

{% block body %}
    <h1>Enseignant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseignant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ enseignant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ enseignant.prenom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ enseignant.mail }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ enseignant.statut }}</td>
            </tr>
            <tr>
                <th>EnSommeil</th>
                <td>{{ enseignant.enSommeil ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Token</th>
                <td>{{ enseignant.token }}</td>
            </tr>
            <tr>
                <th>SaisieFaite</th>
                <td>{{ enseignant.saisieFaite ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>MotDePasse</th>
                <td>{{ enseignant.motDePasse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('enseignant_index') }}">back to list</a>

    <a href="{{ path('enseignant_edit', {'id': enseignant.id}) }}">edit</a>

    {{ include('enseignant/_delete_form.html.twig') }}
{% endblock %}
